node_modules
.next
.vercel
.env
!.env.example
.env.local
.env.*.local
tsconfig.tsbuildinfo
public/pdf.worker.min.mjs

# Local database + client document folders (user data — back up separately)
data/db/
data/clients/
